---
# K8s Databases Role - Percona PG Operator v2.8.2 + Percona MongoDB v1.21.2
# PostgreSQL 18.x via Percona PG Operator, MongoDB 8.0.x via Percona Server for MongoDB
# Tier-aware HA configuration

- name: Set database tier-specific variables
  set_fact:
    db_ns: "databases"
    pg_operator_ver: "2.8.2"
    psmdb_operator_ver: "1.21.2"
    pg_version: "{{ postgresql_version | default(18) }}"
    pg_replicas: "{{ 3 if tier in ['medium', 'production'] else 1 }}"
    pg_storage: "{{ postgresql_storage | default('20Gi') }}"
    pg_proxy_replicas: "{{ 2 if tier in ['medium', 'production'] else 1 }}"
    mongo_replicas: "{{ 3 if tier in ['medium', 'production'] else 1 }}"
    mongo_storage: "{{ mongodb_storage | default('20Gi') }}"

# ============================================
# Percona PostgreSQL Operator
# ============================================

- name: Create databases namespace
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Namespace
      metadata:
        name: "{{ db_ns }}"
        labels:
          app.kubernetes.io/part-of: databases

- name: Add Percona Helm repository
  kubernetes.core.helm_repository:
    name: percona
    repo_url: "https://percona.github.io/percona-helm-charts"

- name: Install Percona PG Operator
  kubernetes.core.helm:
    name: percona-pg-operator
    chart_ref: percona/pg-operator
    chart_version: "{{ pg_operator_ver }}"
    release_namespace: "{{ db_ns }}"
    create_namespace: true
    release_state: present
    wait: true
    timeout: 5m0s

- name: Wait for PG Operator to be ready
  kubernetes.core.k8s_info:
    kind: Deployment
    namespace: "{{ db_ns }}"
    label_selectors:
      - app.kubernetes.io/name=pg-operator
  register: pg_operator_deploy
  until: >
    pg_operator_deploy.resources | length > 0
    and (pg_operator_deploy.resources[0].status.readyReplicas | default(0)) >= 1
  retries: 30
  delay: 10

# ============================================
# PostgreSQL Cluster (PerconaPGCluster CR)
# ============================================

- name: Create PostgreSQL cluster
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: pgv2.percona.com/v2
      kind: PerconaPGCluster
      metadata:
        name: "{{ project_name | default('k8s') }}-pg"
        namespace: "{{ db_ns }}"
      spec:
        crVersion: "{{ pg_operator_ver }}"
        image: "percona/percona-postgresql-operator:{{ pg_operator_ver }}-ppg{{ pg_version }}-postgres"
        postgresVersion: "{{ pg_version }}"
        port: 5432
        instances:
          - name: instance1
            replicas: "{{ pg_replicas }}"
            dataVolumeClaimSpec:
              accessModes:
                - ReadWriteOnce
              storageClassName: "hcloud-volumes"
              resources:
                requests:
                  storage: "{{ pg_storage }}"
            resources:
              requests:
                cpu: "250m"
                memory: "512Mi"
              limits:
                cpu: "1"
                memory: "2Gi"
            affinity:
              podAntiAffinity:
                preferredDuringSchedulingIgnoredDuringExecution:
                  - weight: 100
                    podAffinityTerm:
                      labelSelector:
                        matchLabels:
                          postgres-operator.crunchydata.com/cluster: "{{ project_name | default('k8s') }}-pg"
                      topologyKey: kubernetes.io/hostname
        proxy:
          pgBouncer:
            replicas: "{{ pg_proxy_replicas }}"
            image: "percona/percona-postgresql-operator:{{ pg_operator_ver }}-ppg{{ pg_version }}-pgbouncer"
            config:
              global:
                pool_mode: "transaction"
                max_client_conn: "1000"
                default_pool_size: "25"
            resources:
              requests:
                cpu: "100m"
                memory: "128Mi"
              limits:
                cpu: "500m"
                memory: "512Mi"
            affinity:
              podAntiAffinity:
                preferredDuringSchedulingIgnoredDuringExecution:
                  - weight: 100
                    podAffinityTerm:
                      labelSelector:
                        matchLabels:
                          postgres-operator.crunchydata.com/role: pgbouncer
                      topologyKey: kubernetes.io/hostname
        backups:
          pgbackrest:
            image: "percona/percona-postgresql-operator:{{ pg_operator_ver }}-ppg{{ pg_version }}-pgbackrest"
            repos:
              - name: repo1
                schedules:
                  full: "{{ backup_schedule | default('0 2 * * *') }}"
                  differential: "0 */6 * * *"
                volume:
                  volumeClaimSpec:
                    accessModes:
                      - ReadWriteOnce
                    storageClassName: "hcloud-volumes"
                    resources:
                      requests:
                        storage: "{{ pg_storage }}"
              - name: repo2
                schedules:
                  full: "0 3 * * 0"
                s3:
                  bucket: "backups"
                  endpoint: "http://minio.storage.svc.cluster.local:9000"
                  region: "us-east-1"
            global:
              repo2-path: "/pgbackrest/{{ project_name | default('k8s') }}-pg/repo2"
              repo2-s3-uri-style: "path"
        pmm:
          enabled: false
        users:
          - name: "{{ project_name | default('k8s') }}"
            databases:
              - "{{ project_name | default('k8s') }}"
            options: "SUPERUSER"
          - name: gitlab
            databases:
              - gitlabhq_production

- name: Wait for PostgreSQL cluster to be ready
  kubernetes.core.k8s_info:
    kind: PerconaPGCluster
    namespace: "{{ db_ns }}"
    name: "{{ project_name | default('k8s') }}-pg"
  register: pg_cluster
  until: >
    pg_cluster.resources | length > 0
    and (pg_cluster.resources[0].status.state | default('')) == 'ready'
  retries: 60
  delay: 15
  failed_when: false

# ============================================
# Percona Server for MongoDB Operator
# ============================================

- name: Install Percona MongoDB Operator
  kubernetes.core.helm:
    name: percona-server-mongodb-operator
    chart_ref: percona/psmdb-operator
    chart_version: "{{ psmdb_operator_ver }}"
    release_namespace: "{{ db_ns }}"
    create_namespace: true
    release_state: present
    wait: true
    timeout: 5m0s

- name: Wait for MongoDB Operator to be ready
  kubernetes.core.k8s_info:
    kind: Deployment
    namespace: "{{ db_ns }}"
    label_selectors:
      - app.kubernetes.io/name=psmdb-operator
  register: mongo_operator_deploy
  until: >
    mongo_operator_deploy.resources | length > 0
    and (mongo_operator_deploy.resources[0].status.readyReplicas | default(0)) >= 1
  retries: 30
  delay: 10

# ============================================
# MongoDB Cluster (PerconaServerMongoDB CR)
# ============================================

- name: Create MongoDB cluster
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: psmdb.percona.com/v1
      kind: PerconaServerMongoDB
      metadata:
        name: "{{ project_name | default('k8s') }}-mongo"
        namespace: "{{ db_ns }}"
      spec:
        crVersion: "{{ psmdb_operator_ver }}"
        image: "percona/percona-server-mongodb:8.0"
        imagePullPolicy: IfNotPresent
        replsets:
          - name: rs0
            size: "{{ mongo_replicas }}"
            volumeSpec:
              persistentVolumeClaim:
                storageClassName: "hcloud-volumes"
                accessModes:
                  - ReadWriteOnce
                resources:
                  requests:
                    storage: "{{ mongo_storage }}"
            resources:
              requests:
                cpu: "250m"
                memory: "512Mi"
              limits:
                cpu: "1"
                memory: "2Gi"
            affinity:
              antiAffinityTopologyKey: "kubernetes.io/hostname"
        sharding:
          enabled: false
        mongos:
          size: 0
        backup:
          enabled: true
          image: "percona/percona-backup-mongodb:2.8.0"
          storages:
            s3-minio:
              type: s3
              s3:
                bucket: "backups"
                endpointUrl: "http://minio.storage.svc.cluster.local:9000"
                region: "us-east-1"
                credentialsSecret: minio-backup-credentials
                prefix: "mongodb/{{ project_name | default('k8s') }}-mongo"
          tasks:
            - name: daily-backup
              enabled: true
              schedule: "{{ backup_schedule | default('0 2 * * *') }}"
              keep: "{{ backup_retention_days | default(30) }}"
              storageName: s3-minio
              compressionType: gzip

- name: Create MongoDB backup credentials for MinIO
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: minio-backup-credentials
        namespace: "{{ db_ns }}"
      stringData:
        AWS_ACCESS_KEY_ID: "{{ lookup('env', 'MINIO_ROOT_USER') | default('minioadmin', true) }}"
        AWS_SECRET_ACCESS_KEY: "{{ lookup('env', 'MINIO_ROOT_PASSWORD') | default('minioadmin123', true) }}"

- name: Wait for MongoDB cluster to be ready
  kubernetes.core.k8s_info:
    kind: PerconaServerMongoDB
    namespace: "{{ db_ns }}"
    name: "{{ project_name | default('k8s') }}-mongo"
  register: mongo_cluster
  until: >
    mongo_cluster.resources | length > 0
    and (mongo_cluster.resources[0].status.state | default('')) == 'ready'
  retries: 60
  delay: 15
  failed_when: false

# ============================================
# Cross-namespace connection secrets
# ============================================

- name: Retrieve Percona-generated GitLab PG password
  kubernetes.core.k8s_info:
    kind: Secret
    namespace: "{{ db_ns }}"
    name: "{{ project_name | default('k8s') }}-pg-pguser-gitlab"
  register: gitlab_pg_secret
  until: gitlab_pg_secret.resources | length > 0
  retries: 30
  delay: 10
  failed_when: false

- name: Set GitLab PG password fact
  set_fact:
    gitlab_pg_password: "{{ (gitlab_pg_secret.resources[0].data.password | b64decode) if gitlab_pg_secret.resources | length > 0 else '' }}"

- name: Create PostgreSQL connection secret for GitLab
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: gitlab-postgresql-password
        namespace: gitlab
      type: Opaque
      stringData:
        postgresql-password: "{{ gitlab_pg_password }}"
        host: "{{ project_name | default('k8s') }}-pg-pgbouncer.{{ db_ns }}.svc.cluster.local"
        port: "5432"
        database: gitlabhq_production
        username: gitlab

- name: Display databases summary
  debug:
    msg:
      - "=========================================="
      - "  Databases Deployed"
      - "=========================================="
      - "  PostgreSQL:"
      - "    Percona PG Operator: v{{ pg_operator_ver }}"
      - "    PostgreSQL: {{ pg_version }}"
      - "    Replicas: {{ pg_replicas }}"
      - "    PgBouncer: {{ pg_proxy_replicas }} replicas"
      - "    Cluster: {{ project_name | default('k8s') }}-pg.{{ db_ns }}"
      - "  MongoDB:"
      - "    Percona PSMDB Operator: v{{ psmdb_operator_ver }}"
      - "    MongoDB: 8.0.x"
      - "    Replicas: {{ mongo_replicas }}"
      - "    Cluster: {{ project_name | default('k8s') }}-mongo.{{ db_ns }}"
      - "  Backups: pgbackrest + PBM to MinIO S3"
      - "=========================================="
