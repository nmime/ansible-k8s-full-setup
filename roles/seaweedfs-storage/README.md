# seaweedfs-storage

Legacy SeaweedFS deployment wrapper. Not referenced by the canonical
`deploy_platform.yml` playbook and must not be treated as a second active
object-storage implementation. The active implementation is the
`object-storage` role.
