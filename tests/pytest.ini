# pytest configuration
[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

markers =
    unit: Unit tests (fast, isolated)
    component: Component tests (multi-unit integration)
    e2e: End-to-end tests (full flow validation)
