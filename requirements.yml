---
# Ansible collections requirements
# Install: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: kubernetes.core
    version: ">=3.3.0"
  - name: community.kubernetes
    version: ">=3.2.0"
  - name: community.hetzner
    version: ">=2.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
